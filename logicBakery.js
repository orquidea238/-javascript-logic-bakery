const farine = 1;
const sucre = 1.5 * 2;
const oeufs = 3 * 2;
const lait = 2.5/5;
const chocolat = 10*2;
const decos = 5*3.5;

const prix = farine + sucre + oeufs + lait + chocolat + decos;
console.log(prix);